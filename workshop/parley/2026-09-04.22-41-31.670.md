---
topic: what is "`type Sum = Int` is legal and compiles, but it's just an abbreviation — `Sum` and `Int` are literally interchangeable, so `instance Monoid Sum` *is* `instance Monoid Int`. That defeats the purpose. `newtype` is the construct that gives you a genuinely new type at zero runtime cost, which is why it exists."
file: 2026-09-04.22-41-31.670.md
tags:
---
🌿: 2026-09-04.15-22-10.341_haskell-monads-applicatives.md: Haskell monads and applicatives

💬: what is "`type Sum = Int` is legal and compiles, but it's just an abbreviation — `Sum` and `Int` are literally interchangeable, so `instance Monoid Sum` *is* `instance Monoid Int`. That defeats the purpose. `newtype` is the construct that gives you a genuinely new type at zero runtime cost, which is why it exists."?


💬:

